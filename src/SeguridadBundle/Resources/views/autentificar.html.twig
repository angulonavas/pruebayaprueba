{% extends 'Contenido/raiz.html.twig' %}

{% block breadcrumbs %}

    <div>
        {# Definiendo los nodos del breadcrums #}
        {% set nodos = [
                {
                    'url': 'contenido_raiz',
                    'descripcion': '#', 
                }, 
                {
                    'url': 'usuario_autentificar',
                    'descripcion': 'Login', 
                }, 
            ] 
        %}            
        {% include('Contenido/breadcrums.html.twig') %}
    </div>

{% endblock %}

{% block bloqueB %}
    
	{% if error %}
    	<div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
	{% endif %}

	{% if recordar %}
		{% set chequeado = 'checked' %}
	{% else %}
		{% set chequeado = '' %}
	{% endif %}

	<form action="{{ path('usuario_autentificar') }}" method="post">
	    
	    <label for="username">Username:</label>
	    <input type="text" id="username" name="_username" />

	    <label for="password">Password:</label>
	    <input type="password" id="password" name="_password" />
		
	    <label for="remember_me">Keep me logged in</label>	
	    <input type="checkbox" id="remember_me" name="_remember_me" {{chequeado}} />

	    <button type="submit">login</button>
	   	<a href="{{path('usuario_olvido')}}">Olvido</button>
		<a href="{{path('usuario_registrar')}}">Regístrate</a>

	</form>

{% endblock %}
