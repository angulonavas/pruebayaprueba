{% extends 'Contenido/raiz.html.twig' %}

{% block breadcrumbs %}

    <div>
        {# Definiendo los nodos del breadcrums #}
        {% set nodos = [
                {
                    'url': 'contenido_raiz',
                    'descripcion': '#', 
                }, 
                {
                    'url': 'usuario_recuperar',
                    'descripcion': 'Recuperar credenciales', 
                }, 
            ] 
        %}            
        {% include('Contenido/breadcrums.html.twig') %}
    </div>

{% endblock %}

{% block bloqueB %}
    
    {% if not recuperacion %}
	    <div>
    		No se ha podido recuperar tu entorno. Por favor, revisa tu email de recuperación de claves
    	</div>
    {% else %}

		<div>Hola {{usuario.nombre}}, aquí puedes actualizar tu clave:</div>

		<div>
			{{ form_start(form) }}
			{{ form_widget(form) }}
			{{ form_end(form) }}
		</div>

	{% endif %}

{% endblock %}
   
