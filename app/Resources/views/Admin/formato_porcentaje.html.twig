{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}

	{% if (field_description.options.campo == 'iva') %}
		<span>{{ object.getIva() ~ ' %' }}</span>
	{% endif %}

{% endblock %}